# Garbage Collector

The **garbage collector (GC)** is an algorithm that periodically scans the heap and tags unreferenced variables as free memory, making that memory available for reuse.

## See Also
- [[runtime]] — heap the GC scans
- [[garbage-collection]] — GC algorithms in depth (canonical)
- [[03-computer-systems/operating-systems/memory-management|Memory Management (OS)]] — OS-level memory reclamation
- [[immutability]] — allocation pressure from copies
